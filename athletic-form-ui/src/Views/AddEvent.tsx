import { Grid } from '@mui/material';

interface Props {}

export const AddEvent : React.FC<Props> = () => {
    return (
       <Grid>
           <h1>Add Event</h1>
           <p>This is the Add Event Page</p>
       </Grid>
    );
}